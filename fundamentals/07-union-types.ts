
// Using the Typescript Union Types - Nullable Variables


let uniqueIdentifier: number | string = 1000;

uniqueIdentifier = 3000;

const keys: (number | string) [] = [100, "Damn"];

let courseID: number | null =100;

courseID =null;



// Strict Null Checks and the strictNullChecks Compiler Flag

let cID: number | null = 100;

cID = null;